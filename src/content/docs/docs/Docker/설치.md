---
title: 설치
description: 설치
---

docker 설치
```
sudo apt update
sudo apt install docker.io
sudo systemctl enable --now docker
```

docker compose 설치
```
sudo apt update
sudo apt install docker-compose-plugin
```